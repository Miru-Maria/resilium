GDPR consent bypass via direct /assess navigation	High	A user who navigates directly to /assess (via URL, browser history, or a shared link) skips the /consent page entirely — the consent API call (POST /api/gdpr/consent) never fires and no consent log entry is created. Fix: check for a sessionStorage consent flag in assessment.tsx; redirect to /consent if absent. Set the flag in consent.tsx on "Accept & Continue".
3-second loading spinner before assessment content appears	High	The assessment page waits for Clerk's isLoaded flag with a 3-second timeout before rendering anything. All users see a blank screen with only a spinner for 3 seconds. Fix: reduce the timeout in assessment.tsx from 3000 ms to ~500 ms and show a branded "Preparing your assessment…" intermediate state instead of a blank spinner.
Admin analytics: ageBracket always empty	Medium	The demographics chart for age bracket returns [] even though age group is collected at Step 2 of the assessment. Fix: add the missing GROUP BY age_bracket aggregation to the analytics SQL query in api-server/src/routes/admin/index.ts. The pattern already exists for incomeStability, healthStatus, etc.
Admin analytics: primaryGoalDistribution always empty	Medium	The primary goal selected at Step 14 of the assessment is never surfaced in the admin analytics. Fix: verify the goal field is being persisted to the DB on submission in resilience/index.ts, then add it to the analytics aggregation query.
Admin Reports tab: no search, filter, or pagination beyond 50	Medium	The Reports tab pulls from recentReports inside the analytics response (capped at 50). No dedicated reports endpoint, no filter by date/score/location, no search by report ID. Fix: create GET /api/admin/reports?page&limit&search&minScore&maxScore returning { reports, total, page, limit }, and update the dashboard Reports tab to use it with pagination controls.
Coaching page missing main navigation header	Medium	The coaching page (/coaching) shows only a "← Back to Resilium" button — no About, Demo, or Pricing nav links. A user who lands on coaching via a direct link or referral has no way to browse the rest of the site. Fix: add the main nav to the coaching page header, or pass a showNav prop to the shared header component.
Location data stored unnormalized	Medium	Assessment location answers are stored as raw user input ("Romania", "Bucharest", "Bucharest, Romania", "New York, USA"), causing the same country to appear as multiple entries in the demographics analytics. Fix: normalize server-side on assessment submission — extract a canonical country field and store it alongside the raw string. Update the analytics query to group by normalized country.
No "Sign In" link in the global header nav	Low	The header only shows About, Demo, Pricing. Returning users who have a Clerk account must know to type /sign-in directly — there is no visible sign-in entry point in the nav. Fix: add a "Sign In" ghost button to the header for unauthenticated users, replacing it with the profile dropdown when signed in.
"Sample Plan" vs "Demo" label mismatch	Low	The landing page CTA reads "See a Sample Plan" but navigates to /demo, which is labeled "Demo" in the nav. Fix: align the labels — either rename the nav link to "Sample Plan" or change the button text to "See a Demo."
Push notification permission prompt	Medium	Infrastructure fully built (token storage, broadcast API, push send). The in-app permission request screen hasn't been surfaced to users yet — needs a well-timed prompt (e.g. after first assessment).
Score trend history chart (Pro)	Medium	progressSnapshotsTable captures score snapshots on each assessment. No UI chart to visualize score progress over multiple assessments yet — the data is there, the display layer isn't.
Mobile onboarding flow	Low–Medium	No first-launch walkthrough for new mobile users — they land cold on the Home tab with no contextual guidance on what to do first.